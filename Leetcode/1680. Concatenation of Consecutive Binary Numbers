Example 1:

Input: n = 1
Output: 1
Explanation: "1" in binary corresponds to the decimal value 1. 
-------------------------
class Solution {
    public int concatenatedBinary(int n) {
        final int MOD = 1_000_000_007;
        long ans = 0;
        int bits = 0;

        for (int i = 1; i <= n; i++) {
            if ((i & (i - 1)) == 0) bits++; // power of 2 => new bit length
            ans = ((ans << bits) + i) % MOD;
        }
        return (int) ans;
    }
}
