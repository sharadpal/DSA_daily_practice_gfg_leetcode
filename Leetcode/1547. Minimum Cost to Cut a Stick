Input: n = 7, cuts = [1,3,4,5]
Output: 16
Explanation: Using cuts order = [1, 3, 4, 5] as in the input leads to the following scenario:
------------------------------------------------------------------------------------------------


class Solution {
    public int minCost(int n, int[] cuts) {
        int m=cuts.length;
        int []arr= new int[m+2];
        arr[0]=0;
        arr[m+1]=n;
        for(int i=0;i<m;i++){
            arr[i+1]=cuts[i];
        }
        Arrays.sort(arr);
        int [][] dp= new int[m+2][m+2];
        for(int i=0;i<m+2;i++){
            for(int j=0;j+i<m+2;j++){
                int k=j+i;
                dp[j][k]=Integer.MAX_VALUE;
                for(int l=j+1;l<k;l++){
                    int cost=dp[j][l]+dp[l][k]+(arr[k]-arr[j]);
                    dp[j][k]=Math.min(dp[j][k],cost);
                }
                if(dp[j][k]==Integer.MAX_VALUE) dp[j][k]=0;            
                
            }
        }
        return dp[0][m+1];
    }
}
